{
  "name": "events",
  "description": "Asana Example - Using the streaming API for change events",
  "main": "events.js",
  "private": true,
  "scripts": {
    "start": "node events.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "asana": "git://github.com/Asana/node-asana.git#stream",
    "express": "^4.10.7"
  }
}
